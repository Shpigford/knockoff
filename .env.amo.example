# AMO (Firefox Add-ons) API credentials for scripts/release-firefox.sh.
# Generate at https://addons.mozilla.org/en-US/developers/addon/api/key/
# "JWT issuer" (looks like user:12345678:123) and "JWT secret" (64 hex chars).
AMO_JWT_ISSUER=
AMO_JWT_SECRET=
